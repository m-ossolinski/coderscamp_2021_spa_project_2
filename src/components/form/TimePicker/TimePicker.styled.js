import styled from "styled-components";

export const StyledTimePicker = styled.div`
  display: flex;
  flex-direction: column;
  width: 150px;

  & > label {
    font-size: 0.7rem;
  }

  & > input {
    margin-top: 0.5rem;
  }
`;
