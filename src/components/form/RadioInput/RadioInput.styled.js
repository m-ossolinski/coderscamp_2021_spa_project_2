import styled from "styled-components";

export const StyledRadioInput = styled.div`
  display: flex;
  align-items: center;
  margin: 0.5rem;

  & div {
    margin: 0.5rem 1rem;
  }

  & > p {
    font-size: 0.7rem;
  }

  & input {
    margin-right: 0.5rem;
  }
`;
